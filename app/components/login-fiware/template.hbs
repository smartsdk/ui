<form {{action "authenticate" on="submit"}} class="text-left">
  <p class="text-center">
    <button disabled={{waiting}} class="btn btn-primary" {{action "authenticate"}}>
      <i class="icon icon-fiware"></i> {{t 'loginFiware.buttonText'}}
    </button>
  </p>
</form>
